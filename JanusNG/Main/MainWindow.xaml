<Window x:Class="Rsdn.JanusNG.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:localmain="clr-namespace:Rsdn.JanusNG.Main"
        xmlns:msgview="clr-namespace:Rsdn.JanusNG.MessageView"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="1200"
        Loaded="WindowLoaded">
	<Window.DataContext>
		<localmain:MainModel x:Name="Model" />
	</Window.DataContext>
	<Window.Resources>
	</Window.Resources>
	<Grid Margin="4">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Click="SignInClick" Margin="8" Visibility="{Binding IsNotSignedIn, Converter={StaticResource BoolConverter}}">
				Sign In
			</Button>
			<StackPanel Visibility="{Binding IsSignedIn, Converter={StaticResource BoolConverter}}" Orientation="Horizontal" Margin="4">
				<Ellipse Width="32" Height="32" Margin="4">
					<Ellipse.Fill>
						<ImageBrush ImageSource="{Binding Me.GravatarHash, Converter={StaticResource GravatarConverterSmall}}" />
					</Ellipse.Fill>
				</Ellipse>
				<TextBlock Text="{Binding Me.DisplayName}" VerticalAlignment="Center"/>
			</StackPanel>
		</StackPanel>
		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="4" />
				<ColumnDefinition Width="3*" />
			</Grid.ColumnDefinitions>
			<ListView Name="ForumsList" ItemsSource="{Binding Forums}" SelectionChanged="ForumsSelectionChanged">
				<ListView.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding Name}" />
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
			<GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
			<Grid Grid.Column="2">
				<Grid.RowDefinitions>
					<RowDefinition Height="1*" />
					<RowDefinition Height="4" />
					<RowDefinition Height="2*" />
				</Grid.RowDefinitions>
				<ListView Name="MessagesList" ItemsSource="{Binding Topics}" SelectionChanged="MessageSelectionChanged">
					<ListView.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Subject}" />
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
				<GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" />
				<msgview:MessageView Grid.Row="2" Message="{Binding Message}"/>
			</Grid>
		</Grid>
	</Grid>
</Window>